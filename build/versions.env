# Pin these for byte-for-byte reproducible builds. Bump intentionally and rebuild twice to verify.
ARDUINO_CLI_VERSION=1.2.0
ESP32_CORE_VERSION=3.3.6
# Vendored under libraries/GFX_Library_for_Arduino (library.properties version):
GFX_LIBRARY_VERSION=1.6.5
# Stable epoch for any toolchain that honors SOURCE_DATE_EPOCH
SOURCE_DATE_EPOCH=1700000000
