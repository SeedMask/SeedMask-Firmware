# Name,   Type, SubType, Offset,  Size, Flags
# 4 MB flash — ONE large app (~3 MB) + SPIFFS. Use when firmware no longer fits in dual-OTA slots (~1.25 MB each).
# Arduino IDE: Tools → Partition Scheme → "Custom" (uses this partitions.csv in the sketch folder).
# OTA / dual-slot layout: rename partitions_ota_dual_4mb.csv → partitions.csv (binary must stay <= ~1.2 MB).
nvs,      data, nvs,     0x9000,  0x6000,
phy_init, data, phy,     0xf000,  0x1000,
factory,  app,  factory, 0x10000, 0x2F0000,
spiffs,   data, spiffs,  0x300000,0x100000,
